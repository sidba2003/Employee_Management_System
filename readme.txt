A LAMP (MAMP for MacOS and XAMP for Windows) environment will be required to run this web app locally.
Upon seting up the environment a database will need to be created. To do the same, got ./db/ems.sql and run the script on the respective environment to generate all the required tables.
The entry point of the program is index.html locatde in the root folder.
